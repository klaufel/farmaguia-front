export type pharmacy = {
  address: string;
  city: string;
  coordinates: [number, number];
  email: string;
  guards: Date[];
  id: string;
  name: string;
  phone: string;
  province: string;
  schedule: [string[][][][][][][]];
  web: string;
  zipCode: string;
};
